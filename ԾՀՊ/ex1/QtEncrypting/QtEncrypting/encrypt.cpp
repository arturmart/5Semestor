#include "encrypt.h"

QString EncryptTest::Encrypt(const QString& text) {
    return "test En " + text;
}

QString EncryptTest::Decrypt(const QString& text) {
    return "test Dc " + text;
}
